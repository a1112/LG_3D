由于我不小心调用了 deleteCoilByCoilId(12711)  在 app/algorithm_runtime 编写一个新的 文件 修改 ImageMosaicThread 逻辑， 使 这个脚本根据2个数据库的差异来重新运
  行历史数据。

方法为 从 大到小 遍历 SecondaryCoil 表，检查 Coil 表是否存在 改记录，没有就 重新运行 逻辑

对 ImageMosaicThread 进行结构优化


QML 界面中 列表的右键菜单增加重新识别功能， algorithm_runtime 增加fastapi 的服务端口 增加指定的 数据重新计数请求，
对于列表本身的右键弹窗，全部重新识别选项则是对整个列表中的数据进行重新识别，并且绑定好重新识别的进度数据，重新识别的优先级在新的数据之后